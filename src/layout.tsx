export const metadata = {
  title: "GRAFAM",
  description: "Grace Faith Missions International",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
